# Notification Service Dependencies

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Kafka Consumer
aiokafka==0.10.0           # Async Kafka client for consuming reminder events

# Email Notifications
sendgrid>=6.11.0           # SendGrid API for email delivery

# Push Notifications
firebase-admin>=6.3.0      # Firebase Admin SDK for push notifications

# Database & Configuration
sqlmodel>=0.0.14           # Database ORM (for notification_logs table)
psycopg2-binary>=2.9.9     # PostgreSQL adapter
python-dotenv>=1.0.0       # Environment variable management
pydantic-settings>=2.1.0   # Settings management

# Utilities
python-dateutil>=2.8.2     # Date/time utilities for timezone conversion

# Phase 11: Production Readiness (Observability)
structlog>=24.1.0          # T182: Structured JSON logging with trace_id
prometheus_client>=0.19.0  # T187: Prometheus metrics export

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock==3.12.0
